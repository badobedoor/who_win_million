class PlayerAccount {
  static String? playerEmail;
  static String? emailType;
  static String? playerName;
  static int? playerId;

  PlayerAccount({playerEmail, emailType, playerId, playerName});
}
